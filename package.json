{
  "name": "livedot",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.5",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:server": "bun run --filter server dev",
    "dev:web": "bun run --filter web dev",
    "db:generate": "bunx drizzle-kit@latest generate",
    "db:push": "bunx drizzle-kit@latest push"
  },
  "devDependencies": {
    "turbo": "^2.8.20"
  }
}
